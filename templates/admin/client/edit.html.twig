{% extends 'admin/layout.html.twig' %}

{% set page_title = client.identifier ~ ' bewerken' %}

{% block body %}
    <div class="form">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="button-group">
                <button type="submit">Opslaan</button>
                <a href="{{ path('admin_client_show', { 'id': client.identifier }) }}">Annuleren</a>
            </div>
        {{ form_end(form) }}
    </div>

{% endblock %}